from pathlib import Path


def generate_video(image_path: str, output_path: str) -> str:
    Path("outputs").mkdir(exist_ok=True)
    # Video generation placeholder — implement with fal-ai/kling or runway when needed
    return output_path
